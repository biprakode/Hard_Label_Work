# Hard_Label_Work — Python dependencies
# Tested with Python 3.11. Install with:
#   pip install -r requirements.txt
#
# torch and tensorflow intentionally unpinned here because the compatible
# versions vary by CUDA toolkit. Tested combinations:
#   torch 2.2.x + tensorflow 2.15.x  (CUDA 11.8 / 12.1, CPU-only also works)
#   torch 2.3.x + tensorflow 2.16.x  (CUDA 12.1)
# If you see import errors after installing, pin to one of the above pairs.

torch>=2.2
tensorflow>=2.15         # keras is bundled; do NOT install standalone keras separately
numpy>=1.24
scipy>=1.11
scikit-learn>=1.3
pandas>=2.0
tabulate>=0.9
